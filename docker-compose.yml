version: '3'
services:
  server:
    build:
      context: .
      dockerfile: docker/server/Dockerfile
    environment:
      - REST_HOST=:9090
  client:
    build:
      context: .
      dockerfile: docker/client/Dockerfile
    environment:
      - REST_HOST=server:9090